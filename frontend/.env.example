# Frontend Environment Configuration

# API Base URL (use /api for proxied requests through nginx)
VITE_API_BASE_URL=/api

# Uncomment if you need to connect directly to backend (development)
# VITE_API_BASE_URL=http://localhost:8000
